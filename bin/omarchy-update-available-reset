#!/bin/bash

# omarchy:summary=Ensure Waybar icon offering the available update is removed

source "$OMARCHY_PATH/install/helpers/gb10-lifecycle.sh"
omarchy-guard-gb10-lifecycle "Update-state reset" || exit 1

pkill -RTMIN+7 waybar
exit 0
